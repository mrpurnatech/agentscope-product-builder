Run the AgentScope Product Builder CLI.

Steps:
1. Check that `.env` exists and has `ANTHROPIC_API_KEY` set
2. Ensure the virtual environment is activated: `source .venv/bin/activate`
3. Run `python main.py`
4. Report the build result including repo URL, file count, quality score, and cost
