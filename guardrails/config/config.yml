models:
  - type: main
    engine: anthropic
    model: claude-haiku-4-5

rails:
  input:
    flows:
      - check prompt injection
      - check off topic
      - check malicious content

  output:
    flows:
      - check sensitive data
      - check malicious repo name
      - check hardcoded secrets